# UniVis 开发进度与规划

> 最后更新：2026-04-30 | 当前版本：v0.1.0
> 本文件供 AI 助手在新会话中快速了解项目全貌，每次开发结束后更新。

---

## 1. 项目概况

| 字段 | 内容 |
|------|------|
| 名称 | UniVis — Transformer 推理冗余诊断与可视化工具 |
| 定位 | Python SDK + React Dashboard + 独立 HTML 报告，三者可独立使用 |
| 类型 | 中山大学大学生创新训练项目（大创） |
| 核心能力 | 通过 PyTorch forward_hook 采集 Transformer 各层推理指标，以热力图可视化"哪些层在积极计算、哪些层冗余" |
| 结题标准 | 可安装、可运行、有真实分析价值 |

**设计文档**：`PRD.md`（产品需求）、`ARCHITECTURE.md`（技术架构）、`CLAUDE.md`（开发规范）

---

## 2. 系统架构

```
用户代码
│  import univis
│  tracker = univis.attach(model)
│  # 方式 A：手动循环 + tracker.on_step()
│  # 方式 B：model.generate(logits_processor=[tracker.logits_processor(tokenizer)])
│  tracker.finish()  → 生成 HTML 报告
│
▼
┌─────────────────────────────────────────────────┐
│                 univis SDK (Python)               │
│                                                   │
│  detection.py → probe.py → metrics.py → transport.py
│  (自动检测模型)  (注册hook)  (计算指标)  (JSONL/HTTP推送)
│                                                   │
│  tracker.py (用户 API)  report.py (HTML报告生成)   │
│  __main__.py (CLI入口)  server.py (FastAPI服务)    │
└───────────────────────┬─────────────────────────┘
                        │ HTTP POST → Server → WebSocket
                        ▼
                ┌──────────────────┐
                │   React Dashboard │
                │  (Vite + ECharts) │
                │  热力图/统计/过滤  │
                └──────────────────┘
```

三条独立使用路径：
1. **纯 SDK**：`attach()` → `on_step()` → `finish()` → HTML 报告（不依赖 Server/Dashboard）
2. **SDK + Server + Dashboard**：实时可视化推送
3. **CLI**：`python -m univis serve` 或 `python -m univis report <jsonl>`

---

## 3. 已完成功能（按阶段）

### Phase 0：Hook 可行性验证 ✅
- 验证 `register_forward_hook` 可在 Transformer 模型上工作
- 测量 hook 开销（tiny-gpt2 上 45.8%，属最差情况，真实模型预期 <15%）
- 脚本：`phase0_verify.py`

### Phase 1：SDK 核心 ✅
| 模块 | 文件 | 职责 |
|------|------|------|
| 指标计算 | `metrics.py` | 4 个纯函数：relative_delta、cosine_sim、sparsity、entropy。支持 batch>1 聚合 |
| 模型检测 | `detection.py` | 自动识别 GPT-2/LLaMA/Qwen/BERT 架构的 block 前缀 |
| 探针 | `probe.py` | 注册 forward_hook，缓冲每步指标，flush_step() 返回数据 |
| 传输 | `transport.py` | FileTransport (JSONL)、HttpPushTransport、MultiTransport |
| 用户 API | `tracker.py` | Tracker 类：on_step()、finish()、logits_processor()、remove() |
| 公开接口 | `__init__.py` | `univis.attach()` 工厂函数 |

### Phase 2：Server ✅
| 文件 | 功能 |
|------|------|
| `server.py` | FastAPI：POST /api/push、WS /ws/{id}、GET /api/sessions、GET /api/health。含 CORS、错误处理、优雅关闭 |

### Phase 3：Dashboard ✅
| 文件 | 功能 |
|------|------|
| `App.tsx` | 主框架：连接管理、session 选择、WebSocket、导出报告按钮 |
| `HeatmapView.tsx` | ECharts 热力图（X=token, Y=layer, 颜色=relative_delta），详细 tooltip |
| `StatsPanel.tsx` | 实时统计面板（tokens、avg delta、entropy、VRAM） |
| `TokenList.tsx` | 生成 token 标签列表 |
| `LayerFilter.tsx` | 层选择过滤器（多选框，All/None 快捷按钮） |
| `types.ts` | TypeScript 类型定义 |

### 增强功能 ✅
| 功能 | 说明 |
|------|------|
| ECharts 报告 | `report.py` 生成自包含 HTML（嵌入 ECharts CDN），含热力图+熵曲线+层汇总表+token 显示 |
| generate() 支持 | `tracker.logits_processor(tokenizer)` 返回 HuggingFace 兼容的 logits processor |
| Batch 聚合 | metrics 函数支持 batch>1（per-item 计算 → mean），batch=1 数值完全一致 |
| CLI 入口 | `python -m univis serve` / `python -m univis report <jsonl>` |
| XSS 防护 | 报告中 JSON 数据转义 `</script>` |

---

## 4. 文件清单

### SDK (src/univis/)
```
__init__.py      # 公开 API：attach()
__main__.py      # CLI 入口：serve / report
detection.py     # 模型架构自动检测
metrics.py       # 4 个指标计算函数
probe.py         # forward_hook 注册与缓冲
report.py        # ECharts HTML 报告生成
server.py        # FastAPI WebSocket 服务
tracker.py       # Tracker 用户 API + create_tracker 工厂
transport.py     # 传输层：File / HTTP / Multi
```

### Dashboard (dashboard/src/)
```
App.tsx                          # 主应用框架
main.tsx                         # Vite 入口
types.ts                         # TS 类型定义
components/HeatmapView.tsx       # ECharts 热力图
components/StatsPanel.tsx        # 统计面板
components/TokenList.tsx         # Token 列表
components/LayerFilter.tsx       # 层过滤器
```

### 测试 (tests/) — 67 个测试
```
test_metrics.py      # 18 tests：基础指标 + batch 聚合
test_probe.py        # 8 tests：检测（GPT-2+LLaMA）、hook、transport、MultiTransport
test_report.py       # 13 tests：数据提取、HTML 生成、XSS、离线 ECharts
test_server.py       # 11 tests：health、push、sessions、session 验证、历史上限
test_tracker.py      # 7 tests：logits_processor、报告集成、remove() 清理
test_integration.py  # 6 tests：SDK 全链路、CLI 报告、数据一致性
test_three_end.py    # 4 tests：SDK→Server→WebSocket 三端联调
```

### 示例 (examples/)
```
gpt2_basic.py       # tiny-gpt2 手动推理循环 demo
qwen_basic.py       # Qwen2.5-0.5B benchmark 脚本（L20 上跑）
qwen_generate.py    # Qwen2.5-0.5B + model.generate() + logits_processor
verify_features.py  # 三功能验证脚本（batch=1/batch=2 对比、报告生成、processor）
```

---

## 5. 关键设计决策

| 决策 | 原因 |
|------|------|
| hook 注册在 Transformer block 层而非 attention/mlp | block 级别的 input→output 变化最能反映"层冗余"概念，且兼容所有 Transformer 架构 |
| metrics 函数处理 3D→2D 时取 `[:, -1, :]` | 推理时 last token position 是生成位置，最相关 |
| batch 聚合用 per-item→mean 而非全局 norm | 对 batch=1 数值完全一致，对 batch>1 语义正确 |
| logits_processor 用闭包而非继承 LogitsProcessor | 不依赖 transformers 库，鸭子类型足够 |
| SDK→Server 用 HTTP POST 而非直接 WebSocket | SDK 端无需维护长连接，更简单可靠 |
| 报告用 ECharts CDN 嵌入 | HTML 自包含（无外部文件依赖），但需联网加载 JS |
| report.py 的 `_safe_json` 转义 `</script>` | 防止 token 文本中的 `</script>` 破坏 HTML 结构 |

---

## 6. 已知限制（MVP 范围内可接受）

| 限制 | 说明 |
|------|------|
| batch_size=1 | PRD 明确 MVP 只支持 batch=1。logits_processor 只 decode `next_token_id[0]` |
| ECharts CDN 依赖 | 报告需联网加载 ECharts JS，离线环境不可用 |
| hook 开销未在真实模型验证 | ✅ L20 上 Qwen2.5-0.5B/3B/7B 全部验证通过 |
| Dashboard 未联调 | ✅ 已通过 test_three_end.py 验证 SDK→Server→WebSocket 全链路 |
| 无 beam search 支持 | logits_processor 仅适用于 greedy/sampling |

---

## 7. 开发环境

### Windows（当前机器）
- **问题**：PyTorch DLL 加载失败（`fbgemm.dll`），无法在 Windows 原生跑测试
- **解决**：所有测试和运行都在 WSL 里完成

### WSL Ubuntu
```bash
source /home/wqf18/anaconda3/etc/profile.d/conda.sh && conda activate douban_crawler
cd /mnt/d/code/univis

# 安装（开发模式）
pip install -e .

# 跑测试
python -m pytest tests/ -v

# 验证功能
python examples/verify_features.py
```

### 代理与镜像
- **HuggingFace 镜像**：`export HF_ENDPOINT=https://hf-mirror.com`（示例脚本已内置）
- **WSL 代理**：`export http_proxy=http://$(ip route show default | awk '{print $3}'):7897`
- **小模型**：`sshleifer/tiny-gpt2`（5MB，测试用）
- **真实模型**：Qwen2.5-0.5B-Instruct（L20 服务器，已下载）

### 常用命令
```bash
# SDK 开发
pip install -e ".[dev]" && pytest tests/ -v

# Server
python -m univis serve --port 8765

# Dashboard
cd dashboard/ && npm install && npm run dev    # 开发服务器 :5173

# CLI 生成报告
python -m univis report univis_data_xxxx.jsonl -o report.html
```

---

## 8. 踩坑记录

| 问题 | 原因 | 解决 |
|------|------|------|
| Windows PyTorch fbgemm.dll 加载失败 | Windows base conda 环境缺少 C++ 运行时 | 改用 WSL |
| WSL 无网络 | 默认 NAT 模式需要手动配代理 | `export http_proxy=http://$(ip route show default \| awk '{print $3}'):7897` |
| HuggingFace 下载超时 | 国内网络被墙 | 脚本内置 `HF_ENDPOINT=https://hf-mirror.com` |
| json.dumps 不转义 `</script>` | 生成的 token 可能包含 HTML 标签 | `_safe_json()` 函数转义 `</script>` → `<\\/script` |
| 多行 python -c 在终端粘贴缩进错乱 | 终端逐行处理导致格式丢失 | 写成独立 .py 文件再运行 |
| git 仓库初始化在错误层级 | D:\code 下没有 .git | 在 D:\code\univis 内 git init |

---

## 9. 下一步开发规划

按优先级排序，供后续 AI 会话参考：

### P0：必做（大创结题硬性要求）

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 1 | **真实模型验证** | ✅ 完成 | L20 跑通 Qwen2.5-0.5B + logits_processor，24层×50步=1200数据点，报告45KB |
| 2 | **三端联调** | ✅ 完成 | `test_three_end.py` 4 个端到端测试验证 SDK→Server→WebSocket 全链路 |
| 3 | **pyproject.toml 完善** | ✅ 完成 | entry_points、classifiers、optional deps、project URLs |
| 4 | **README.md** | ✅ 完成 | 英文专业 README，含 badges、quick start 3 模式、安装、CLI、metrics 表 |

### P1：高价值（提升项目完整度）

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 5 | **更大模型测试** | ✅ 完成 | Qwen2.5-7B-Instruct (36层×10步) L20 验证通过，报告 1MB。0.5B/3B/7B 三模型全部验证 |
| 6 | **Dashboard 构建优化** | ✅ 完成 | ECharts vendor chunk 拆分，WS proxy，es2020 target |
| 7 | **集成测试** | ✅ 完成 | `test_integration.py` 6 个端到端测试，覆盖 SDK→JSONL→report 全链路 |

### P2：锦上添花（时间和精力允许）

| # | 任务 | 预估 | 说明 |
|---|------|------|------|
| 8 | **离线 ECharts** | ✅ 完成 | echarts.min.js 内联打包，offline=True 参数，tracker 默认离线 |
| 9 | **层间对比视图** | 2h | Dashboard 新增组件：选择两个 layer 显示其 delta 对比曲线 |

### Dashboard 视觉升级（v0.3）
| # | 功能 | 说明 |
|---|------|------|
| 12 | **热力图升级** | plasma 色图 + 动画 + dataZoom 缩放 + 增强工具提示 + 自动滚动 |
| 13 | **字体升级** | DM Sans (UI) + JetBrains Mono (数据) via Google Fonts |
| 14 | **Token 熵值着色** | 按 entropy 着色（蓝→紫→橙），点击高亮热力图对应列 |
| 15 | **统计面板动画** | 计数器滚动动画 + 语义颜色编码 + 主指标突出 |
| 16 | **首页/Hero 状态** | 连接前引导界面 + 等待数据脉冲动画 + 淡入过渡 |
| 10 | **PRD/ARCHITECTURE 文档更新** | ✅ 完成 | v0.2，所有 MVP 功能标 ✅，新增 6 个 v0.2 特性，L20 benchmark 数据 |
| 11 | **GitHub Actions CI** | 0.5h | 自动跑 pytest + tsc --noEmit |

### 建议的下一个 AI 会话起点
所有 P0/P1/P2 列表中的任务已全部完成。Code review 和安全加固也已完成。下一步方向：

1. **GitHub 发布准备** — 推送到远程仓库、创建 GitHub Release、打包 PyPI。
2. **大创结题材料** — 演示 PPT、项目报告文档。

---

## 10. Git 历史

```
d448433 feat: security hardening, code review fixes, three-end integration tests
1a3dc9c docs: update progress — all P0/P1/P2 tasks complete
cd56c9f feat: offline ECharts, Vite build optimization, docs sync, 3B model verified
6edcffb feat: packaging, README, integration tests, full-stack smoke test
894f99e docs: add development progress summary and next-step planning
cdecc20 feat: add CLI entry point, server hardening, dashboard enhancements, generate example
86059a8 feat: add ECharts report, generate() auto-hook, and batch aggregation
3db1c31 feat: UniVis v0.1.0 — Transformer inference redundancy diagnostic toolkit
```

分支策略：`master` 为开发主线，`main` 为远程主分支。当前所有提交在 `master`。

---

## 11. Code Review 记录（2026-04-30）

### CRITICAL（已修复）
| 问题 | 文件 | 修复 |
|------|------|------|
| innerHTML XSS | report.py | 改用 createElement + textContent |
| 无 session_id 验证 | server.py | 正则校验 `^[a-zA-Z0-9_-]{1,64}$` |

### HIGH（已修复）
| 问题 | 文件 | 修复 |
|------|------|------|
| flush_step 未注入 token_index | probe.py | 循环注入 entry['token_idx'] |
| 历史消息无上限 | server.py | `_MAX_HISTORY = 10000` + 裁切 |
| visualMap max=0 边界 | report.py | `max(max_delta * 1.1, 0.001)` |
| subscriber 清理竞态 | server.py | 列表推导替换逐个 remove |

### MEDIUM（已修复）
| 问题 | 文件 | 修复 |
|------|------|------|
| detection 无匹配时静默 | detection.py | 添加 logger.warning |
| token_idx 缺失 KeyError | report.py | `.get()` 带回退值 |
| jinja2 未使用 | pyproject.toml | 移除依赖 |

---

## 更新日志

- **2026-05-01**：Dashboard 视觉升级。热力图 plasma 色图+动画+缩放、DM Sans/JetBrains Mono 字体、Token 熵值着色+点击交互、统计面板动画+颜色编码、Hero/等待状态。TypeScript 0 错误，67 测试通过。
- **2026-04-30 (4)**：P1/P2 全部完成。离线 ECharts、Vite 构建优化、PRD/ARCHITECTURE 文档同步到 v0.2。Qwen2.5-3B (36层) L20 验证通过。55 测试通过。
- **2026-04-30 (2)**：进度文档创建。
- **2026-04-30 (1)**：初版。完成 Phase 0-3 + 全部增强功能，45 测试通过。
